<div id="favorite-<%= @noodle.id %>">
  <% if !current_user.nil? && current_user.favorite?(@noodle) %>
    <%= render "users/unfavorite" %>
  <% else %>
    <%= render "users/favorite" %>
  <% end %>
</div>
